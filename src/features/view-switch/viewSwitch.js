import React from "react";

const initialState = {}

const viewSwitch = (state = initialState, {type, payload}) => {
  switch (type) {
    default:
      return state;
  }
}

export default viewSwitch;